# 專案偏好與工作方式備份

這份文件是在把資料夾從「My Project」改名成「副食品app」**之前**手動整理的備份,原因是 Claude Code 針對這個專案記住的偏好(內部的 memory 系統)是綁定在**舊資料夾路徑**上的,改名後新的一輪對話很可能不會自動帶到這些偏好,所以先寫成一份看得到、找得到的文件留底。如果之後開新對話時 Claude 好像不記得這些,把這份文件內容貼給它或請它讀取這份文件即可。

備份時間:2026-08-02

---

## 我對 Claude 的偏好(原本記在 Claude 的 memory 系統裡)

1. **一律用繁體中文回覆**,不要簡體中文。英文技術詞彙可以保留英文,但中文一律要繁體。
   - 緣由:曾經收到簡體中文回覆後明確糾正過。

2. **任何有互動元素(表單、勾選、輸入框、日曆、可編輯清單等)的 Artifact 網頁,預設就要自動把使用者輸入存進 localStorage**,不用每次特別交代。純顯示用(圖表、靜態報表)的頁面不需要這樣做。
   - 緣由:希望重新整理頁面或關掉瀏覽器後,填過的資料不會不見,且目前只需要「同瀏覽器/同裝置」的持久化,不需要雲端同步。

3. **本機預覽優先的工作流程**(這次副食品 app 開發過程中建立起來的默契,雖然沒有正式存成 memory,但整個對話都是照這個模式走):
   - 改動「寶寶副食品紀錄.html」之後,一定要先重新產生「寶寶副食品紀錄_本機預覽.html」讓我在本機看
   - 發布到 Artifact 之前,一律要先跑過驗證(括號/引號平衡、重複 id、HTML 標籤平衡、icon 對應、食材數量是否還是 271、`deno check` 驗證 JS 語法沒有壞掉)
   - 只有在我明確說「發布」「同步到 artifact」之類的話之後,才會真的發布到 Artifact,不會自己擅自發布
   - 比較複雜、需要先討論設計方向的新功能(例如多計量單位那次),Claude 會先用 Plan Mode 提出方案討論,確認過再動手;簡單明確的小修改則直接做

---

## 副食品 App 目前狀態

這個專案本身已經有完整的文件記錄目前狀態,**這些文件本身就在資料夾裡,改名/搬移資料夾不會受影響、也不會遺失**,不需要靠這份備份文件重複整理一次:

- **`生成計劃.md`**:整個網頁功能的完整說明文件,依主題分類(整體架構、每日紀錄、食材紀錄、進度總覽、試敏指南、使用導覽、常見度系統等),隨每次改版更新
- **`願望清單.md`**:目前受限於 Artifact 架構、還做不到但之後想做的功能(照片辨識成分、拍照辨識商品、跟「愛托付」App 同步)
- **`副食品資料庫.md`**:271 項食材的原始資料來源與常見度標記,改這份文件再跟 Claude 說「同步到網頁」就能更新 Artifact
- **`使用說明.md`**:給使用者(妳的朋友們)看的白話版操作說明

**目前已發布的 Artifact 連結**(這個不會因為改資料夾名稱而改變或消失):
https://claude.ai/code/artifact/5b5316fd-36de-4b93-a67d-16a311afaf93

---

## 關於改名這件事的小提醒

VS Code 左側 Explorer 最上面顯示的資料夾名稱(原本是「MY PROJECT」),還有 Claude Code 側邊的 session 歷史清單(副食品_0722、副食品_0723 等舊對話),都是跟著資料夾**路徑**走的,不是跟著資料夾本身。改名之後:

- 這份文件、`生成計劃.md`、`寶寶副食品紀錄.html` 等所有實體檔案都完整保留,不受影響
- 但 session 歷史清單裡,改名前的舊對話記錄,在改名後的新專案裡可能會找不到、對不上(資料本身沒有被刪除,只是撈取方式跟路徑綁在一起,換了路徑等於換了一個新的分類抽屜)
- 如果之後想回顧某次舊對話的細節,這份文件加上 `生成計劃.md` 應該已經涵蓋大部分重點;真的需要挖更細節的話,可以再跟 Claude 說,由它幫忙想辦法查找舊資料
